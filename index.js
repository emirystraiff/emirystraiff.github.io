$('.imgPOPUP').magnificPopup({
    type: 'image',
        image: {
            titleSrc: 'title' 
            // this tells the script which attribute has your caption
        }
    // other options
  });
